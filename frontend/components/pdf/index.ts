// Re-export PDF viewer components.
// Uses browser-native PDF rendering (iframe) — no external dependencies.
export {
  PDFViewerModal,
  PDFPreview,
  type PDFViewerModalProps,
  type PDFPreviewProps,
} from "./PDFViewerModal";
